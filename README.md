$.fn.jqPlotToImage() is a [jQuery](http://jQuery.com) extension for [jqPlot](http://www.jqplot.com/) that allows one to export a plot as an image
that can be saved to persistent storage as a PNG file or printed.
